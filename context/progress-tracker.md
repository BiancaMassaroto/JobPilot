# Progress Tracker

Update this file after every completed feature. Any AI agent reading this should immediately know what is done, what is in progress, and what is next.

---

## Current Status

**Phase:** Phase 4 — Job Details Page
**Last completed:** 12 Job Details Page — Full UI (built + self-checked; not yet click-tested through the real running app, same sandbox limitation as every prior feature)
**Next:** 13 Company Research Agent

---

## Progress

### Phase 1 — Foundation

- [x] 01 Homepage
- [x] 02 Auth
- [x] 03 PostHog Initialization
- [x] 04 Database Schema

### Phase 2 — Profile Page

- [x] 05 Profile Page — Full UI
- [x] 06 Profile Save Logic
- [x] 07 AI Profile Extraction from Resume
- [x] 08 Resume PDF Generation from Profile

### Phase 3 — Find Jobs Page

- [x] 09 Find Jobs Page — Full UI
- [x] 10 Adzuna Job Discovery
- [x] 11 Filter + Sort + Pagination

### Phase 4 — Job Details Page

- [x] 12 Job Details Page — Full UI
- [ ] 13 Company Research Agent

### Phase 5 — Dashboard

- [ ] 14 Dashboard Page — Full UI
- [ ] 15 Stats Bar — Real Data
- [ ] 16 Recent Activity — Real Data
- [ ] 17 Analytics Charts — PostHog Data

---

## Decisions Made During Build

### 01 Homepage

- Built against `context/designs/landing-page.png` (source of truth per ui-rules.md) rather than build-plan.md's shorter description — the approved design has **two** feature sections with three value props each ("Manage Your Job Search With Ease" and "Apply With More Confidence, Every Time"), not one. Mapped to `Features.tsx` and `HowItWorks.tsx` per architecture.md's homepage component list.
- Added two homepage components not listed in architecture.md's folder plan, needed to match the design: `Testimonial.tsx` and `CtaSection.tsx`.
- Design's dashboard/table/table/terminal mockups are pre-made static assets (`public/images/dashboard-demo.png`, `jobs-lists.png`, `agnet-log.png`) that already pixel-match the design — used directly via `next/image` instead of rebuilding as HTML/CSS.
- Design's dark CTA buttons ("Get Started", hero + bottom CTA) use a subtle gradient not in ui-tokens.md's documented button spec (which only defines the purple `accent` primary button). Sampled the design's pixel colors and matched them to existing tokens: `from-text-darker to-text-slate` gradient for hero/bottom-CTA buttons, flat `bg-text-slate` for the navbar "Start for free" button — no new hex introduced.
- Hero and bottom-CTA sections use a soft radial-gradient background (pink/blue blur) per the design, built from `var(--color-accent-light)` / `var(--color-info-light)` tokens via an arbitrary Tailwind `bg-[radial-gradient(...)]` value — not a flat white card. This is a deliberate exception to ui-rules.md's "cards are always white" rule since it's a marketing hero, not a data card.
- `Get Started` / `Find Your First Match` / `Start for free` all link to `/login` for now. The build-plan's authenticated-vs-unauthenticated redirect logic depends on InsForge auth, which is Feature 02 — not yet built.
- Installed `lucide-react` (already an approved dependency in code-standards.md) for the play-triangle icon on CTA buttons.
- Logo uses `public/logo.png` directly (icon + wordmark baked into one asset) rather than rebuilding the gradient-square icon in code — simpler and pixel-matches the design; ui-tokens.md's Logo spec (gradient square + separate text) can be revisited later if the logo needs to render at other sizes/colors.
- Navbar is a Client Component (`"use client"`) because it uses `usePathname` to support active-link highlighting for when Dashboard/Find Jobs/Profile pages exist.
- Added an accessible mobile navigation toggle to `Navbar`: below `md`, the menu exposes Dashboard, Find Jobs, and Profile while preserving the existing Start for free CTA.

### 02 Auth

- **Corrected a stale package name across architecture.md, library-docs.md, and code-standards.md**: those files referenced `@insforge/ssr` as a standalone npm package. It does not exist (confirmed 404 on the npm registry). The real package is `@insforge/sdk` (already the one AGENTS.md's own InsForge instructions and the MCP `fetch-docs` tool point to); its SSR helpers live at the `@insforge/sdk/ssr` and `@insforge/sdk/ssr/middleware` subpaths — verified against the installed package's own bundled `SDK-REFERENCE.md`. All three context files and the actual code now use the correct import path.
- **`middleware.ts` → `proxy.ts`**: this project runs Next.js 16.3.4, which renamed Middleware to Proxy (confirmed in `node_modules/next/dist/docs/01-app/01-getting-started/16-proxy.md` per AGENTS.md's "read the bundled docs first" rule). architecture.md said `middleware.ts` — corrected to `proxy.ts` with an exported `proxy()` function. The InsForge SDK's own `updateSession()` reference already anticipates this rename.
- **OAuth callback is a Route Handler (`callback/route.ts`), not a page** (architecture.md originally listed `callback/page.tsx`). Exchanging the OAuth code and setting the resulting session cookies requires writing `Set-Cookie` headers, which a Server Component page cannot do — only a Route Handler or Server Action can. Corrected in architecture.md's folder plan.
- Session model: cookie-based SSR mode (`createBrowserClient()` / `createServerClient()` from `@insforge/sdk/ssr`), not the plain `createClient()` + in-memory-session pattern from the generic InsForge SDK docs. This keeps the refresh token httpOnly and server-owned, matching architecture.md's existing two-client-instance design. Because of this, sign-in/sign-out run through Server Actions (`actions/auth.ts`) via `createAuthActions()`, and the OAuth flow is: Server Action calls `signInWithOAuth(provider, { skipBrowserRedirect: true })` → stores `codeVerifier` in an httpOnly cookie → redirects to the provider → `callback/route.ts` calls `exchangeOAuthCode()` and sets the session cookies → redirects to `/dashboard`.
- `proxy.ts` protects `/dashboard`, `/profile`, `/find-jobs` by checking the `accessToken` that `updateSession()` returns (it transparently refreshes an expired access token from the httpOnly refresh cookie first, so a real session is never bounced to `/login` just because the short-lived access token expired).
- Completed the Feature 01 logic that was deferred pending this feature: `Home` (`app/page.tsx`) is now async and redirects logged-in users to `/dashboard` server-side (`project-overview.md`'s documented homepage flow). The homepage CTAs ("Get Started", "Start for free") now link straight to `/dashboard`/`/find-jobs` instead of `/login` — `proxy.ts` itself bounces unauthenticated visitors to `/login`, so no separate auth check was needed in those components.
- lucide-react (v1.41.0, already installed) ships no Google/GitHub brand marks — inlined both as local, unexported SVG components in `LoginCard.tsx` rather than adding a new icon dependency.
- **BLOCKING before release, not just a manual step to remember:** the backend's `allowedRedirectUrls` is currently empty (confirmed via `get-backend-metadata`). Until it's populated, OAuth doesn't degrade, it fails outright at the callback for every provider — no user can sign in with Google or GitHub at all. Add `http://localhost:3000/callback` for local dev and the deployed origin's `/callback` before release, in the InsForge dashboard's Auth settings, then **actually test a sign-in with Google and with GitHub** against each configured origin to confirm the callback completes end-to-end (session cookies set, landed on `/dashboard`), not just that the setting was saved.
- Redesigned the login page as a two-panel card (pitch panel + sign-in panel) to match a design reference shared in chat, replacing the original single centered card. See ui-registry.md's `LoginCard` entry.
- **`/code-review` pass — fixes applied:**
  - `app/(auth)/callback/route.ts` had no try/catch (code-standards.md requires one on every route handler) — wrapped the whole handler; a thrown error now falls back to the same `/login?error=oauth_failed` redirect as a normal exchange failure.
  - On a failed `exchangeOAuthCode`, the `insforge_oauth_verifier` cookie deletion had been applied to the discarded success-path `response` object, not the `NextResponse` actually returned — the stale verifier cookie would survive a failed attempt. Fixed to delete it on whichever response is actually returned.
  - `actions/auth.ts`'s `getAppOrigin()` built the OAuth `redirectTo` straight from the request's `Host` header, which a proxy/misconfigured LB can spoof — open-redirect risk until `allowedRedirectUrls` (see above) is populated. It now requires a validated `NEXT_PUBLIC_APP_URL` outside local development and only permits a loopback `Host` fallback in `NODE_ENV=development`. Documented in code-standards.md's env var table; set in `.env.local`.
  - `text-white` (a raw Tailwind class, against ui-rules.md's "project tokens only" rule) on the Navbar/Hero/CtaSection CTA buttons — swapped for the existing `text-accent-foreground` token (`#ffffff`), no new token needed.
  - Hero.tsx and CtaSection.tsx hand-copied the identical CTA button markup — ui-registry.md had already flagged this as a "reuse, don't invent" case that never got followed. Extracted into `components/homepage/CtaButtons.tsx`, now the single source both files import.
  - The review also caught that `package.json`/`context/ui-tokens.md`/`context/ui-rules.md` commit to Tailwind v4 (`@theme`, no `tailwind.config.ts`) while AGENTS.md's InsForge setup notes said "EXTRA IMPORTANT: Use Tailwind CSS 3.4 (do not upgrade to v4)". Flagged to the user rather than resolved unilaterally, since downgrading would mean rewriting Feature 01's entire token system. **Decision: keep v4** — it's the one actually shipped and working since Feature 01. AGENTS.md's note is corrected to say so explicitly, with a pointer to `ui-tokens.md`/`ui-rules.md`.

### 03 PostHog Initialization

- **Corrected a stale client-init pattern across architecture.md and library-docs.md**: those files documented a manual `posthog.init()` inside `lib/posthog-client.ts`, wired up from the root layout, reading `NEXT_PUBLIC_POSTHOG_KEY`. An `instrumentation-client.ts` file already existed at the project root (from an earlier `@posthog/wizard` run, per the installed `integration-nextjs-app-router` skill's bundled PostHog docs) using the current recommended Next.js integration — the `instrumentation-client.ts` file convention, confirmed as the real one in `node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/instrumentation-client.md` (introduced Next.js 15.3, auto-loaded before hydration, no import needed anywhere) — and reading `NEXT_PUBLIC_POSTHOG_PROJECT_TOKEN`. Kept that file as-is and repointed `lib/posthog-client.ts` to a narrower job: identity helpers only (`identifyUser`, `resetIdentity`), not init. Corrected `architecture.md`'s folder plan, `library-docs.md`'s PostHog section, and `code-standards.md`'s env var table to match.
- **Two events already existed outside the approved list**: `components/auth/LoginCard.tsx` and `components/homepage/CtaButtons.tsx` (built in Features 01–02) already called `posthog.capture()` for `oauth_sign_in_started` and `marketing_cta_clicked`, neither of which was in `code-standards.md`'s 4-event list. Asked the user — decision: keep both, add them to the approved list rather than strip them out. `code-standards.md` now documents six events.
- Added `lib/posthog-server.ts` — `createPostHogServer()` factory per the documented pattern (`flushAt: 1`, `flushInterval: 0`), reading the corrected env var name. Installed `posthog-node` (already an approved dependency in code-standards.md, just not yet installed).
- `posthog.identify()` after login: this app's OAuth flow is entirely server-side (Server Action → provider → Route Handler callback → redirect), so there is no client-side "login succeeded" moment to hook into directly. Added `components/auth/PostHogIdentify.tsx` — a headless client component mounted once in the root layout — that resolves the current session via `insforge.auth.getCurrentUser()` on every page load and calls `identifyUser()` if one exists. This is the documented fallback pattern (PostHog's own Next.js integration docs, "identify as soon as you're able," for apps that learn the user asynchronously) rather than the `posthog.init({ loaded })` callback, which only works for synchronously-known users.
- **`posthog.reset()` on logout — not wired yet, flagged rather than forced**: `signOutAction` in `actions/auth.ts` exists (Feature 02) but has no caller anywhere in the UI yet; there is no sign-out control (the Navbar isn't session-aware). Building a logout button now would be scope creep beyond this feature. `resetIdentity()` is exported from `lib/posthog-client.ts` and ready — wire a call to it into whichever client component ends up submitting `signOutAction`, whenever that control is built.
- No `ui-registry.md` entry: `PostHogIdentify` renders `null` and has no visual footprint, so there's no pattern to register there.
- **`/code-review` pass — fixes applied:**
  - **BLOCKING before merge — live API key exposure, not resolved by the `.gitignore` fix alone:** `.mcp.json` carries a live InsForge API key in plaintext and wasn't covered by `.gitignore` — added an ignore rule so it can't be committed going forward. Checked whether it already had been: `git log --all -- .mcp.json` and `git ls-files` both come back empty, so nothing needs purging from repository history here. But the `.gitignore` rule only stops a *future* commit — it does not revoke the key, and the file has sat in plaintext on disk this whole time, readable by anything with local filesystem access (backup/sync tools, other local processes, a shared screen), not only git. **Rotate this key in the InsForge dashboard before merge** and update the local `.mcp.json` with the new value; treat the current key as compromised regardless of the clean git history. If a copy of it ever does turn up in git history, a CI log, or a shared copy elsewhere, purge it from that history too and rotate again.
  - `app/globals.css`'s plain `@theme` block redefined `--font-sans` as a literal `"Inter", sans-serif` string, colliding with the identically-named CSS variable next/font's `inter.variable` also writes on `<html>` — and since no `@font-face` exists for the literal name "Inter", this meant the self-hosted font was never actually rendering, regardless of which declaration won the cascade. Renamed the next/font variable to `--font-inter` (`app/layout.tsx`) and added a separate `@theme inline { --font-sans: var(--font-inter), sans-serif; }` block so `--font-sans` explicitly resolves to next/font's real generated stack. Verified in the compiled CSS: `.font-sans{font-family:var(--font-inter), sans-serif}`.
  - `instrumentation-client.ts` threw an unguarded error at module scope when PostHog env vars were missing in development — since Next.js auto-loads this file before hydration on every page, a fresh clone without `.env.local` configured crashed the whole app for what should be optional analytics. Changed to `console.warn`.
  - `signOutAction` never unlinked the browser's PostHog identity. Added `components/auth/SignOutButton.tsx`, wrapping the action with `resetIdentity()` — not consumed anywhere yet (still no sign-out control in the UI), but now the correct call site exists for whenever one is built, instead of relying on a future caller to remember the pairing.
  - `LoginCard.tsx`'s `oauth_sign_in_started` capture fired in `onSubmit` immediately before the Server Action's hard redirect to the OAuth provider — an async `posthog.capture()` racing page unload can be aborted mid-flight. Added `captureBeforeNavigate()` (`lib/posthog-client.ts`), which fires the capture then resolves after a short delay; both OAuth forms now `preventDefault()`, call it, then `requestSubmit()` once it resolves.
  - `signInWithOAuthAction` and `signOutAction` returned `Promise<void>` against code-standards.md's `{ success, error? }` convention for Server Actions. Both actions redirect unconditionally on every path (`redirect()` itself is typed `never`), so there's no code path that returns a value — retyped both `Promise<never>` and documented this as the one sanctioned exception in code-standards.md, rather than force a return value that would never be reached.
  - The OAuth verifier cookie name was hardcoded independently in `actions/auth.ts` and `app/(auth)/callback/route.ts`. Tried exporting it from `actions/auth.ts` first — broke the build, because a `"use server"` file's exports are all treated as Server Action references and can't include a plain constant. Moved it to a new `lib/auth-constants.ts` instead, imported by both.
  - `isPostHogConfigured` was computed with an identical inline expression independently in `LoginCard.tsx` and `CtaButtons.tsx` (a third variant lived in `instrumentation-client.ts`, left as is since its job — deciding whether to init at all — is a different concern). Consolidated the two component-side checks into a single `isPostHogConfigured` export in `lib/posthog-client.ts`.
  - Google's brand colors were hardcoded as raw hex in `LoginCard.tsx`'s inline SVG with no stated exception to ui-tokens.md's "never hardcode hex" invariant. Added the brand-icon exception to `ui-tokens.md` and a short comment at the SVG — these are fixed trademarked colors, not design tokens, and shouldn't be swapped for one.

### 04 Database Schema

- **`/architect` pass first**: the build-plan.md entry left real decisions open — whether to add Postgres RLS (InsForge genuinely supports it, confirmed live via `auth.uid()`/`auth.role()`/`auth.jwt()`), how to handle a real gap in InsForge's storage isolation (`storage.objects` has RLS disabled with no per-object ownership check), whether `jobs.source = 'url'` should stay reserved despite URL import being out of scope, and cascade delete behavior. Full rationale lives in `architecture.md`'s "Constraints, Row Level Security & Migration" section.
- Ran the migration via the `run-raw-sql` MCP tool: `profiles`, `agent_runs`, `jobs`, `agent_logs` created with all columns, CHECK constraints, foreign keys, and indexes exactly as decided. `profiles.id REFERENCES auth.users(id)` — the flagged cross-schema FK risk — was accepted by InsForge without issue, so the documented fallback (a plain `uuid` PK with no FK into `auth`) wasn't needed.
- RLS enabled and policies verified on all four tables via `get-table-schema` (`rlsEnabled: true`, `auth.uid() = user_id` / `= id` policies present on each).
- Created the `resumes` storage bucket via `create-bucket` (`isPublic: false`).
- **Verified end-to-end**, not just via the admin MCP path: ran a real query through the app's actual `@insforge/sdk` client using the public `NEXT_PUBLIC_INSFORGE_URL` + anon key from `.env.local` (the same config `lib/insforge-client.ts` uses) — `select()` on `profiles` returned `{ data: [], error: null }` with no session, confirming RLS blocks anonymous reads correctly rather than erroring or leaking rows.
- `jobs.company_research`, `agent_logs.job_id`, and other nullable columns match architecture.md exactly — no column list drift from what `/architect` specified.

### 05 Profile Page — Full UI

- Built pixel-by-pixel against `context/designs/profile.png` (source of truth per ui-rules.md), not build-plan.md's text description — the approved design has **no Cover Letter Tone field** in Job Preferences, unlike build-plan.md's description and architecture.md's `profiles.cover_letter_tone` column. Left it out of the UI entirely (input coverage for this feature is "what the design shows"); the DB column stays reserved for whenever a later feature actually surfaces it.
- Two new tokens added to `ui-tokens.md` / `globals.css` — `--color-error-light: #ffe2e2` (completion ring track) and `--color-error-lightest: #fef2f3` (missing-field badge background) — sampled directly from the design PNG. Neither existed before; the existing `--color-error` (#ef4444) already matched the ring fill and badge text/icon exactly, so that one was reused as-is.
- No shadcn/ui components used — followed Features 01/02's established pattern (hand-rolled Tailwind matching ui-tokens.md/ui-rules.md exactly) rather than introducing shadcn now, since nothing in the codebase uses it yet and `components/ui/` is reserved for it per architecture.md's system boundaries. Revisit if a future feature actually needs shadcn primitives.
- Extracted five reusable, non-exported-elsewhere components under `components/profile/` beyond architecture.md's four (`ProfileForm`, `ResumeUpload`, `CompletionIndicator` — `ResumePreview` deferred, see below): `TextField`, `TextAreaField`, `SelectField` (generic field chrome, reused ~16 times), `TagInput` (shared by Skills + Industries), `WorkExperienceRoleCard` (one role, rendered up to 3 times). Same pattern as Feature 01 adding `CtaButtons.tsx`/`Testimonial.tsx` beyond the original folder plan — extracted because the alternative was hand-copying the same field markup a dozen-plus times in one file.
- `ResumePreview.tsx` (listed in architecture.md's folder plan) **not built** — the design has no "resume already uploaded" visual state to build against, and build-plan.md Feature 05 only describes the empty upload state. Deferred to whichever of Feature 07/08 first needs to render an uploaded/generated resume.
- Work Experience list is fully interactive (add up to 3 roles, remove any role once more than one exists) even though the design's mock only shows one role — the "+ Add role" control the design does show has no purpose if roles can't actually be added and removed. The remove (trash icon) affordance itself isn't in the design; added it because the design has no way to build a role-removal UI reference for something it never shows more than one of.
- All profile state is local `useState`, seeded with the design's own mock values (Faizan Ali / Vercel / etc.) verbatim — no InsForge reads/writes yet (Feature 06), so the Save Profile and Generate Resume from Profile buttons render but do nothing yet.
- Verified with `npm run build` (typecheck + production build, both clean) and `npm run lint` (no warnings). Visual comparison against the design was done by hand (pixel-sampling the design PNG with PIL for exact colors/positions) rather than a rendered screenshot diff — this sandbox has no headless browser (`chromium-cli`/Playwright/Chromium all unavailable) to drive the real `/profile` route, which is also behind `proxy.ts` auth and has no session to satisfy in this environment. Confirmed the route renders correctly (200, expected section text present, no error markers) via a temporary unauthenticated preview route that was deleted before finishing.

### 07 AI Profile Extraction from Resume

- Built exactly to `architecture.md`'s Feature 07 decision (`/architect`, 2026-09-07): `lib/gemini.ts`, `lib/profile-extraction-schema.ts` (new — the shared zod schema, `normalizeStringList`, `normalizeWorkExperience`), `extractProfileFromResumeAction` in `actions/profile.ts`, `ExtractableProfileFields` in `types/index.ts`, and the `ResumeUpload`/`ProfileForm` wiring (second `useActionState`, `formAction` submit button).
- **Live-verified corrections, not just checked against docs — the decision's own "verify against the installed package" instruction, followed through with real API calls, not just type definitions:**
  - **`pdf-parse` v2.4.5 (the installed version) is a different API than `library-docs.md` documented.** No default-exported `pdf(buffer)` function (that's v1) — it's `new PDFParse({ data: buffer })`, `await parser.getText()` → `.text`, `await parser.destroy()`. Confirmed against the installed package's own README/types and a direct import smoke test. `library-docs.md`'s pdf-parse section corrected.
  - **`gemini-2.5-flash` returned a live `404`** — "no longer available to new users, use models/gemini-3.6-flash". Switched to `gemini-3.6-flash`, confirmed working end to end with real resume text against the real API (not a mock).
  - **`responseSchema` doesn't cleanly typecheck with a `z.toJSONSchema()` object — `responseJsonSchema` (typed `unknown`) is the right field.** Confirmed in the installed `@google/genai` package's own types/doc comments.
  - **`gemini-3.6-flash` is a thinking model; thinking tokens count against `maxOutputTokens`.** A live run's `usageMetadata` showed ~1300 thinking tokens alone for a short sample resume — `maxOutputTokens: 800` (this decision's original number, carried over from the GPT-4o budget table) silently truncated the JSON mid-string. Raised to `3000` after confirming that clears a full extraction in testing. Thinking cannot be turned off on this model — `thinkingConfig: { thinkingBudget: 0 }` returned a live `400`.
  - **The provisioned `GEMINI_API_KEY` is free-tier, capped at 20 requests/day for this model** — confirmed via a live `429` mid-testing. Not a code problem, but **flagging before real usage**: every extraction past the 20th that day currently fails with the generic "try again or fill in manually" message, no quota-specific signal to the user. Upgrade the key's billing plan before this ships to real users.
  - All of the above verified end-to-end against the real Gemini API during this build (not the SDK's types alone) — full detail and the corrected code snippet in `library-docs.md`'s Gemini section; `architecture.md`'s Feature 07 decision (Decisions 3–4) carries pointers to the same corrections.
  - **A live run also caught a real extraction bug**, not just a docs mismatch: Gemini sometimes writes `"Present"` into `endDate` even when it correctly sets `currentlyWorkingHere: true`. The schema's per-field `.catch("")` (swapped in for the original `z.union([regex, literal("")])`, since a malformed value should fall back to empty rather than fail the whole `safeParse` — the decision's own stated intent) can't see across fields, so added `normalizeWorkExperience()` (`lib/profile-extraction-schema.ts`) to force `endDate: ""` whenever `currentlyWorkingHere` is `true`, applied after schema validation alongside the existing skills/industries normalization.
- Verified with `npx tsc --noEmit` (clean), `npm run lint` (clean), `npm run build` (clean production build), and three live end-to-end `generateContent()` calls against the real Gemini API using the actual shipped schema/model/config (not a mock) — two succeeded with valid, schema-passing JSON; a third hit the free-tier quota (see above), not a code defect. No UI screenshot: same sandbox limitation as Feature 05 (no headless browser, `/profile` is behind `proxy.ts` auth) — the Extract button, loading state, and error rendering were verified by reading the rendered JSX logic against `ResumeUpload.tsx`'s existing (already-working) upload UI patterns, not by driving a real browser.
- No new PostHog event, no DB write, no `revalidatePath` — matches Decision 10/12 exactly (this action only feeds `ProfileForm`'s local state; Save Profile, a separate action, is unchanged).
- **One deviation from Decision 11's exact code, required by this project's own lint config, not a spec disagreement:** the decision's `useEffect` that merges `extractState.data` into `profile` state failed this project's `react-hooks/set-state-in-effect` lint rule (`eslint-config-next` 16) as a hard error — calling `setState` synchronously inside an effect. Replaced with React's own documented alternative for "adjust state when a value changes": a render-time comparison against a tracked previous value, calling `setProfile` directly during render instead of inside `useEffect`. Same trigger condition (a new `extractState.data` object identity), same unconditional-overwrite behavior, same `experienceLevel` null-coalescing — only the React mechanism changed. `architecture.md` not edited for this one (it's tooling-driven, not a fact about Gemini/pdf-parse to correct project-wide).

**2026-09-07 — Two more bugs surfaced once the engineer actually drove this through the real running app (real browser, real dev server, a real uploaded resume) — the isolated smoke tests above didn't catch either:**

- **The pdf-parse worker setup this feature needed doesn't exist at all outside Next.js.** First real click threw `Setting up fake worker failed: Cannot find module '.../.next/dev/server/chunks/ssr/pdf.worker.mjs'` — `pdf-parse` (built on pdfjs-dist) tries to start a real worker the way it would in a browser, and Turbopack's dev bundle doesn't expose that file where pdfjs-dist looks for it. Fixed with `PDFParse.setWorker(getData())` (`pdf-parse/worker`'s self-contained base64 data URL, not `getPath()`'s filesystem path — same bundler-relocation problem) at module scope in `actions/profile.ts`, before any `new PDFParse(...)` call. That fix then surfaced a second, build-only failure — `pdf-parse/worker` pulls in `@napi-rs/canvas`'s native binding, which Turbopack's production build can't place in an ESM chunk (`non-ecmascript placeable asset`) — fixed with `serverExternalPackages: ["pdf-parse", "@napi-rs/canvas"]` in `next.config.ts` (pdf-parse's own documented Next.js setup, confirmed against its GitHub troubleshooting doc). Both required; neither shows up in a plain Node script, only under real Next.js — `library-docs.md`'s pdf-parse section now has a dedicated "Next.js / Turbopack Setup" subsection with both fixes and a warning not to trust a standalone script's success alone for this library.
- **A real (Portuguese, multi-page) resume truncated the Gemini response at `maxOutputTokens: 3000`** — the isolated testing above only used a short synthetic English resume, which happened to fit. Raised to `8000`. Also added a `finishReason === "MAX_TOKENS"` check before the generic parse-failure log, so a future truncation reads as "truncated by token budget" in the logs instead of looking identical to every other malformed-JSON case. `library-docs.md`'s Gemini section updated with both the new number and the `finishReason` check.
- Net lesson carried forward: an isolated script that imports the same libraries is not equivalent to driving the actual feature through the actual framework runtime — Next.js/Turbopack-specific bundling failures and real-world input variance (a longer, non-English resume needing more thinking tokens) only showed up once this ran for real. Re-verified after both fixes: `npx tsc --noEmit`, `npm run lint`, `npm run build` all clean.
- **Third real-usage finding, same day:** the free-tier quota flagged as a risk during the build (20 requests/day for `gemini-3.6-flash`) was hit for real — between build-time testing and the engineer's own retries, a live `429 RESOURCE_EXHAUSTED` came back through the actual running app, previously only reproduced in isolation. It surfaced as the generic `EXTRACTION_FAILED_ERROR` copy, indistinguishable from a real parsing failure — confirmed confusing in practice, not just a theoretical gap. Added `EXTRACTION_QUOTA_ERROR` ("Extraction is temporarily unavailable (usage limit reached)...") and `isQuotaExceededError()` (`error instanceof ApiError && error.status === 429`, `ApiError` imported from `@google/genai`) to `actions/profile.ts`, checked in the `generateContent` catch block before falling back to the generic message. Detection confirmed against the engineer's own still-exhausted quota (a live 429, not a mock) before reporting this done. This is a third error message beyond architecture.md's Decision 9's originally-specified two — a deliberate, engineer-approved addition (asked first via `AskUserQuestion`), not a unilateral spec change; Decision 9 not rewritten, this note is the record of the addition.

### 08 Resume PDF Generation from Profile

- Built to `architecture.md`'s Feature 08 decision (`/architect`, 2026-09-07, corrected mid-build — see below): `lib/resume-generation-schema.ts` (new — `resumeContentSchema`, `workExperienceCountMatches`), `lib/resume-pdf-template.tsx` (new — the `@react-pdf/renderer` `<Document>` tree), `lib/profile-completion.ts` (new `hasMinimumResumeData` export), `lib/profile-transform.ts` (`ProfileRow` gained `resume_pdf_url`), `app/api/resume/generate/route.ts` (new — `POST` handler), and the `ResumeUpload`/`ProfileForm`/`ResumePreview` wiring.
- **Provider corrected mid-build, at the engineer's direct request:** this decision was originally drafted (and first coded) against GPT-4o, matching `architecture.md`'s Feature 07 decision's "everything except extraction stays on GPT-4o" framing at the time. The engineer then stated the broader policy directly — Gemini for every AI feature, not just extraction — so the already-written `lib/openai.ts` was deleted, `openai` was uninstalled, and the route was rewritten against `lib/gemini.ts` (`gemini-3.6-flash`, `responseJsonSchema`, `maxOutputTokens: 8000` — reusing Feature 07's own live-verified parameters rather than rediscovering them). `architecture.md`'s Feature 07 and Feature 08 decisions, `build-plan.md`, `code-standards.md`, and `library-docs.md` were all updated to match — see the Feature 08 decision, Decision 1, for the full correction. Features 10/13/17 still say GPT-4o in these docs as of this build; that should be revisited when each is actually built.
- **A real installed-type mismatch caught by `tsc`, not assumed from docs:** `@react-pdf/renderer`'s `renderToBuffer()` types require a `ReactElement<DocumentProps>` — i.e. the actual `<Document>` element, not an element for a wrapping component. `ResumePdfDocument` is called as a plain function (`ResumePdfDocument({ profile, email, content })`, not `createElement(ResumePdfDocument, {...})`) so its return value (the real `<Document>` tree) is what gets passed to `renderToBuffer`, keeping `app/api/resume/generate/route.ts` a plain `.ts` file with no JSX of its own. Separately, wrapping the rendered `Buffer` directly in `new Blob([buffer], ...)` failed to typecheck (`Buffer<ArrayBufferLike>` isn't assignable to `BlobPart`'s stricter `ArrayBuffer` requirement under the installed TS lib types) — fixed with `new Blob([new Uint8Array(buffer)], ...)`.
- `ProfileFormData` was **not** extended with `resumePdfUrl` — it's passed as a separate `initialResumePdfUrl` prop to `ProfileForm`, the same treatment as `email` (always sourced from the row/session directly, never round-tripped through the save form's JSON blob).
- `hasMinimumResumeData` (the hard-minimum gate: full name, plus a job title or at least one work-experience entry) operates on the raw DB row, not `ProfileFormData` — the route loads the row directly and never goes through `fromProfileRow` for this check.
- Verified with `npx tsc --noEmit` (clean), `npm run lint` (clean), and `npm run build` (clean production build, `/api/resume/generate` listed as a route) — confirms `@react-pdf/renderer` needed **no** `serverExternalPackages` addition to `next.config.ts`, unlike `pdf-parse`'s Turbopack issue in Feature 07.
- **Confirmed on the engineer's first real click-through, same day: "View PDF" 401'd instead of opening the file.** Same lesson Feature 07 already taught — a clean typecheck/lint/build is not proof a feature works live. Generation itself succeeded (Gemini call, PDF render, and Storage upload all worked — `resume_pdf_url` was populated), but opening the resulting `resume_pdf_url` directly in a new tab returned `{"error": "AUTH_INVALID_CREDENTIALS", "message": "No token provided", "statusCode": 401}` from InsForge's storage API, not the PDF. Root cause: that API requires a real Authorization bearer token, which only the SDK's authenticated HTTP client attaches (`insforge.storage.from(...).download()`) — a plain `<a href>` navigation never carries one, regardless of the browser having a valid app session. This was worse than the "obscurity, not enforcement" access model `architecture.md`'s Feature 04 decision described — a raw link doesn't work for anyone, owner included, not just "works for anyone who knows the path."
  - **Fixed**: new `app/api/resume/download/route.ts` — a same-origin `GET` route that looks up the caller's own `profiles.resume_pdf_url`, derives the storage key back out of it (the SDK's `upload()` URL is `.../objects/${encodeURIComponent(key)}`, confirmed against the installed package — no new DB column needed), calls `.download()` with the authenticated server client, and streams the PDF back with `Content-Type: application/pdf`. `ResumePreview.tsx`'s "View PDF" link now points at `/api/resume/download`, not `resumePdfUrl` directly.
  - `architecture.md`'s "InsForge Storage" section, the Feature 04 decision's "Storage isolation gap" note, the Feature 08 decision's Decision 12, `library-docs.md`'s react-pdf Rules, and `ui-registry.md`'s `ResumePreview` entry were all corrected to match — this access model was wrong in more than one place, not just the one component that first hit it.
  - Re-verified after the fix: `npx tsc --noEmit`, `npm run lint`, `npm run build` all clean, `/api/resume/download` listed as a route.
- **Second live-found bug, same day, on the engineer's very next click:** the auth fix worked (no more 401), but `/api/resume/download` then returned a real `500` — server logs showed `InsForgeError: Object not found (STORAGE_NOT_FOUND, statusCode 404)` from `insforge.storage.from("resumes").download(key)`. The first fix's `key` was reverse-engineered by parsing it back out of `resume_pdf_url` (assuming the URL's `/objects/<encoded-key>` shape round-trips cleanly through `decodeURIComponent`) — that assumption didn't hold up against the real backend, for reasons not fully pinned down (possibly the URL's actual encoding, or the object simply not existing under the assumed key).
  - **Fixed properly instead of re-guessing the encoding:** added `profiles.resume_storage_key` (new column, migration applied live via `run-raw-sql`) and now save the InsForge SDK's own `key` field from the upload response directly — both in `app/api/resume/generate/route.ts` (generation) and `actions/profile.ts` (Feature 06's upload path, which had the exact same latent bug, just never noticed since nothing read it back yet). `app/api/resume/download/route.ts` reads `resume_storage_key` directly now, no URL parsing at all. This is the vendor's own documented pattern ("save both url and key") — should have been done this way from the start rather than reconstructing the key.
  - **Consequence:** any profile whose resume was generated or uploaded before this column existed (including the one used for today's testing) has no key yet and will show "No resume found" until generated or uploaded again. Re-verified after this fix: `npx tsc --noEmit`, `npm run lint`, `npm run build` all clean. **Still not re-confirmed live** (this sandbox has no way to click through it again) — the engineer should click "Generate Resume from Profile" once more (to populate the new key), then "View PDF" to confirm it actually opens the PDF this time.
- **UX issue flagged by the engineer, same day: "Generate Resume from Profile" showed up at the top of the page, above the fields it actually depends on.** It lived in `ResumeUpload.tsx`'s footer (Feature 05's original design placement for what had been an inert button) — but Generate reads whatever is currently *saved*, not unsaved edits below it, so its position invited generating from stale/incomplete data before ever saving. On request, moved to a new "Generate a Resume" section inside `ProfileForm.tsx`'s "Profile Information" card, right after the Save Profile button. `ResumeUpload.tsx` no longer takes any generate-related props — it's upload/extract only now. `architecture.md`'s Feature 08 decision (Decision 11) and `ui-registry.md` updated to match. Re-verified: `npx tsc --noEmit`, `npm run lint`, `npm run build` all clean.
- Both `GEMINI_API_KEY` requests this feature can now make (extraction, generation) share the same free-tier 20-requests/day cap flagged in Feature 07's notes — worth the same "upgrade before real usage" caution, now for two features instead of one.
- No new PostHog event — matches Decision 15 (not in `code-standards.md`'s six-event list, not called for by `build-plan.md`/`project-overview.md`).

### 09 Find Jobs Page — Full UI

- Built pixel-by-pixel against `context/designs/find-jobs.png` (source of truth per ui-rules.md): `app/find-jobs/page.tsx`, `components/find-jobs/{SearchControls,JobFilters,JobsTable,JobsPagination,mock-jobs}.tsx`, `lib/utils.ts` (new — `formatRelativeTime`). All Server Components, no interactivity wired (Feature 10/11's job) — plain `<input>`/`<select>` elements are enough for the UI to look and feel real without any client state.
- **Design has no SOURCE column** (Search/URL badge), unlike build-plan.md's Feature 09 text description (`... SOURCE (Search/URL badge), DATE FOUND`) — initially built to the image exactly (Company, Role, Match Score, Salary Est., Date Found only), same "design PNG overrides build-plan prose" precedent as Features 01/05.
- **2026-09-08 — Added the Source column back in, on direct request, despite the design's omission.** `Job.source: "search" | "url"` added to `mock-jobs.ts` (mirrors the `jobs.source` DB column exactly), rendered via a new local, non-exported `SourceBadge` helper in `JobsTable.tsx` — reuses this page's own already-established pill-badge pairings rather than inventing new colors: `search` → `bg-accent-light text-accent` (same as the `Tailored` status badge in ui-tokens.md), `url` → `bg-surface-secondary text-text-secondary` (same as `Low Match`). Mock data: 5 of 6 rows `"search"`, Notion `"url"`, so both badge states are visible. `ui-registry.md`'s `JobsTable` entry updated. Re-verified: `npx tsc --noEmit`, `npm run lint`, `npm run build` all clean.
- **Corrected two stale/wrong docs, found by pixel-sampling the design image directly (PIL), not by eyeballing:**
  - `ui-tokens.md`'s Match Score Colors table and `ui-rules.md`'s Match Score Bar section **disagreed with each other** (90/70/50 vs 80/60 splits) and **both disagreed with the actual design** — sampled all six row colors: 94/96/91% are `#00BC7D` (`success-alt`), 88/85% are `#2B7FFF` (`info-medium`), 72% is `#FF8904` (`warning`), track is `#E5E7EB` (`border-light`). Real thresholds are 90/80/50, three distinct colors (green/blue/orange), not two greens. Both docs corrected; no new hex introduced, every sampled color matched an existing token exactly.
  - Success banner colors also pixel-verified: bg `#ECFDF5` (`success-lightest`), icon `#00BC7D` (`success-alt`), text `#007A55` (`success-foreground`) — all existing tokens, no doc correction needed here (nothing was previously specified for this banner).
- **Found and fixed a real gap in the shared `Navbar`, not just this page's own UI:** `find-jobs.png` shows each nav item with an icon and the active item with a bottom underline. Checked `dashboard.png` and `profile.png` before touching a shared component — both agree, confirming this is a real cross-page design update the already-shipped Navbar (Features 01/05) never picked up, not a find-jobs-only detail. `ui-rules.md` previously stated "No underline — active state is color change only," which was simply wrong against all three delivered designs. Updated `components/layout/Navbar.tsx` (desktop + mobile nav) with `LayoutGrid`/`Search`/`User` (lucide) icons and a `border-b-2` active underline (transparent when inactive, so layout doesn't shift); corrected `ui-rules.md`'s Navbar section and `ui-registry.md`'s Navbar entry to match. This touches every page that renders `Navbar` (homepage, dashboard, profile, find-jobs) — visually additive only, no props or behavior changed.
- `MatchScoreBar` (the colored progress bar + %) is a local, non-exported helper inside `JobsTable.tsx` — same "unexported local helper colocated with the main export" pattern as `LoginCard.tsx`'s brand SVGs, since it's only used within this one table's row-mapping and isn't a cross-file shared component (yet).
- `Job` type and `MOCK_JOBS` array live in `components/find-jobs/mock-jobs.ts`, scoped to this feature (not `types/index.ts`) since Feature 10/11 hasn't decided the real DB-row-to-UI transform yet — same "don't commit a global type before a second real consumer needs it" precedent as `profile/TextField.tsx`. Field names (`company`, `role`, `matchScore`, `salaryEstimate`, `foundAt`) are chosen to map cleanly onto the `jobs` table's `company`/`title`/`match_score`/`salary`/`found_at` columns, so Feature 10/11's swap to real data should be close to a drop-in.
- `JobsPagination` keeps the design's literal page controls (pages 1‑2‑3‑…‑8) but derives the displayed result range from the persisted job count passed by `JobsListSection`; the real pagination logic (page size, range-generation, ellipsis rules) remains Feature 11's decision.
- No new PostHog event, no DB read/write, no `MATCH_THRESHOLD` constant added yet (`lib/utils.ts` only got `formatRelativeTime`) — all correctly out of scope per build-plan.md's "No logic yet."
- **2026-09-08 — Wired the Match Score sort dropdown, on direct request, ahead of Feature 11.** Sort semantics (`matchScore` descending, `newest`/`oldest` by `foundAt`) are exactly build-plan.md's Feature 11 spec — no open decision, just applied client-side against the Feature 09 mock array instead of a real DB query, since Feature 10 hasn't shipped a real data source yet. New `components/find-jobs/sort-jobs.ts` (`SortOption`, `sortJobs`) and `components/find-jobs/JobsListSection.tsx` (new — `"use client"`, owns `sortBy` state via `useState`/`useMemo`, the only stateful piece on this page). `JobFilters.tsx` is now `"use client"` too (it attaches the `onChange`) and takes `sortBy`/`onSortChange` props; its "All Matches" filter select is untouched (still uncontrolled, no logic) since only sort was asked for. `app/find-jobs/page.tsx` stays a plain Server Component, rendering `<JobsListSection jobs={MOCK_JOBS} />` — the interactivity is isolated to one small client island, not the whole page. Re-verified: `npx tsc --noEmit`, `npm run lint`, `npm run build` all clean.
- Verified with `npx tsc --noEmit` (clean), `npm run lint` (clean), `npm run build` (clean production build, `/find-jobs` listed as a static route). **Not verified in a real browser** — this sandbox has no headless browser binary installed (Playwright's CLI is present but its Chromium build isn't downloaded, and downloading it wasn't run); the engineer should click through `/find-jobs` for a real visual check before moving on, same category of gap as Features 05/07/08.
- **The `/develop` skill installed in `.claude/skills/develop` expects `docs/scope/` + `docs/specs/` (a generic scope/spec bookkeeping system) — this repo has neither and has never used them across 8 prior features.** This project's real, established bookkeeping is this file plus `ui-registry.md`, per AGENTS.md's "Rules That Never Change." Followed the skill's build methodology (the UI bar, phases, accessibility mindset) but wrote status here instead of inventing a parallel `docs/scope` tree. Worth resolving properly later — either point the skill's scope resolution at `context/progress-tracker.md`, or accept that this project intentionally uses its own system instead. Separately, AGENTS.md's "Available Skills" list (`/imprint`, `/review`, `/recover`, `/remember`) doesn't match what's actually installed under `.claude/skills/` (only `/architect`, `/develop`, `/audit`, `/check`, `/debug`, `/document`, `/scope`, `/sync`, `/test`, plus the two tailwind skills) — that section of AGENTS.md is stale/aspirational and should be corrected or the missing skills installed.

### 10 Adzuna Job Discovery

- Built exactly to `architecture.md`'s Adzuna Job Discovery decision (`/architect`, 2026-09-08 — already fully ratified before this build started, so Step 0's spec gate cleared with no new decision needed): `lib/utils.ts` (`MATCH_THRESHOLD`), `lib/job-matching-schema.ts` (new), `lib/adzuna.ts` (new — `searchJobs`, `mapAdzunaJobType`, with the cross-check-corrected `job_type` mapping from `contract_time`, not `contract_type`), `lib/agent-logs.ts` (new — `logAgentError`), `agent/adzuna.ts` (new — `discoverJobs`, `detectAdzunaCountry`), `agent/matcher.ts` (new — `scoreJobs`), `app/api/agent/find/route.ts` (new), `lib/job-transform.ts` (new — `fromJobRow`), `types/index.ts` (`Job`/`JobSource` moved here from `mock-jobs.ts`, plus `FindJobsRequestBody`/`FindJobsResponseData`), and the `SearchControls`/`app/find-jobs/page.tsx` wiring. `context/build-plan.md` and `context/library-docs.md` were already corrected as part of the `/architect` pass itself (Gemini provider, batched scoring, the `job_type` fix) — nothing left for this build to correct there.
- **One real gap in the decision's own stated signature, fixed rather than followed literally:** Decision 9a originally specified `logAgentError(runId, jobId, message, error)`, but `agent_logs.user_id` is `NOT NULL` with no default (`architecture.md`'s Feature 04 migration) — the documented signature alone couldn't satisfy the RLS policy or the column constraint. The five-parameter signature is now documented consistently as `logAgentError(userId, runId, jobId, message, error)`, with `userId` sourced from the same auth check every call site already has.
- **Live-verified against the real APIs, not just typechecked — same bar Features 07/08 set:**
  - **Adzuna**: a real `GET` against `/v1/api/jobs/gb/search/1` (`what=Frontend Engineer&where=London&category=it-jobs`) returned `200` with 10 real results. Confirmed the real response shape matches `lib/adzuna.ts`'s `AdzunaJob` type exactly, including `contract_time: "full_time"` with no `contract_type` present on an ordinary permanent listing — exactly the case Decision 8a's cross-check fix exists for.
  - **Gemini batched scoring**: a real `gemini.models.generateContent()` call against the exact model/schema/prompt/config `agent/matcher.ts` ships (`gemini-3.6-flash`, `responseJsonSchema` from `jobMatchSchema`, `temperature: 0.3`, `maxOutputTokens: 8000`) with two candidate jobs (one a strong match, one a clear mismatch) returned `finishReason: "STOP"` and valid, schema-passing JSON — high score + populated `matchedSkills`/empty `missingSkills` for the matching job, low score + populated `missingSkills` for the mismatched one. Confirms the split "don't invent" prompt constraint (Decision 2's cross-check correction) actually produces the intended asymmetry, not just that it typechecks.
- No new columns, no new tables — reuses `agent_runs`/`jobs`/`agent_logs` exactly as Feature 04 built them (Decision 3).
- `salaryEstimate` falls back to the literal string `"Not disclosed"` (`lib/job-transform.ts`) when a saved job has no `salary` (Adzuna didn't return `salary_min`) — no design mock covers this state; a plain, honest fallback stood in rather than inventing a formatted range. Small, purely-cosmetic default text, not a load-bearing choice — flagged here rather than treated as silent.
- Verified with `npx tsc --noEmit` (clean), `npm run lint` (clean), `npm run build` (clean production build — `/api/agent/find` and `/find-jobs` both listed as routes, `/find-jobs` now dynamic since it reads the session and does a real DB read). **The route was not click-tested through the real running app** — same sandbox limitation as every prior feature (`/find-jobs` is behind `proxy.ts` auth, no headless browser available here). Unlike earlier features, though, this build's core logic (the Adzuna call and the Gemini batched scoring call) was independently confirmed working against the real, live third-party APIs, not just self-consistent in isolation — the remaining unverified surface is narrower: the route's own DB writes (`agent_runs`/`jobs`/`agent_logs` inserts, the dedup query, the composite FK, RLS) and the client-side fetch/banner wiring. The engineer should run one real search from `/find-jobs` and confirm: the success banner text, a new row appearing in the table with no page reload, `agent_runs.status: 'completed'` with the right `jobs_found`, and that a second identical search shows the dedup message instead of duplicate rows — the same checklist as `architecture.md`'s decision's own Verify section.
- **2026-09-09 — Real live failure caught by the engineer clicking Find Jobs: `POST /api/agent/find` 502'd after ~10s.** `agent_logs` had the real cause (queried directly, not guessed): Gemini returned a `503` on `gemini-3.6-flash` — `"This model is currently experiencing high demand... Please try again later."` — a transient upstream outage, not a code defect. `agent_runs.status` correctly showed `'failed'`, no jobs were saved, and the client got the generic message — confirms Decision 9's failure-handling contract works end to end on a real failure, exactly as designed. Since Gemini's own message calls this explicitly temporary, added `withGeminiRetry()` (`lib/gemini.ts`, new) — retries a `503` up to twice with a short delay before the caller's catch block treats it as a real failure; a `429` (the already-known daily quota cap) is deliberately **not** retried, since retrying would just waste another request against the same exhausted cap. Wired into `agent/matcher.ts`'s batched scoring call, the one that actually failed. Also corrected `lib/gemini.ts`'s header comment, which still said Gemini was Feature-07-only/GPT-4o for everything else — stale since Feature 08's project-wide provider switch. Re-verified: `npx tsc --noEmit`, `npm run lint`, `npm run build` all clean. **Not yet re-confirmed live against another real transient 503** (this kind of outage isn't reproducible on demand) — the retry path itself was exercised only by code review, not a live repeat failure.
- **2026-09-08 — Added a `JobsEmptyState` to `JobsListSection.tsx`, on request, from a shared screenshot.** Shown in place of the table and pagination whenever `jobs.length === 0` (no search run yet, or the last one saved nothing new) — an empty `<table>` next to `JobsPagination`'s literal "Showing 1 to 6 of 24 results" text would have actively lied about there being results. Not exported, local to `JobsListSection.tsx`: a `Building2` (lucide) icon in a `h-12 w-12 rounded-full bg-surface-secondary` badge (reuses `ResumeUpload.tsx`'s badge sizing and `JobsTable`'s own company-cell icon, no new pattern invented) above "No jobs found yet. Run a search to get started." (`text-sm text-text-secondary`). See `ui-registry.md`'s `JobsListSection` entry. Re-verified: `npx tsc --noEmit`, `npm run lint` both clean.

_Add notes here as the build progresses — workarounds, patterns, anything that differs from the context files._

- **2026-09-07 — Added `student` as an Experience Level option**, requested directly (not part of a numbered feature): someone who doesn't work yet and hasn't graduated now has a value that fits, instead of being forced into Junior/Mid/Senior/Lead. Applied a live migration (`profiles_experience_level_check` now allows `student`), updated the `ExperienceLevel` type, the zod schema in `actions/profile.ts`, and `EXPERIENCE_LEVEL_OPTIONS` in `ProfileForm.tsx`. Job Title and Years of Experience stay visible but become optional (not required for `is_complete`/the completion percentage) when Student is picked — see architecture.md's Feature 06 decision, "Student exception" addendum, and `lib/profile-completion.ts`. Skills stays required for everyone, students included, per the request.
- **2026-09-07 — Made LinkedIn URL and Portfolio/GitHub optional**, requested directly: both fields relabeled "(Optional)" in `ProfileForm.tsx` and dropped from `lib/profile-completion.ts`'s `REQUIRED_CHECKS` entirely (unconditionally, not just for students). The required-unit count went from 11 to 9 — see architecture.md's Feature 06 decision, "LinkedIn URL / Portfolio-GitHub made optional" addendum.
- **2026-09-07 — Added a student-only Academic Experience section**, requested directly: awards, exchange programs, undergraduate research ("Iniciação Científica," with or without funding/bolsa), and similar. Four judgment calls asked directly rather than assumed (`AskUserQuestion`), all answered with the recommended default: visible only when Experience Level is Student; structured fields (Type, Title, Institution, Year, a Funded/scholarship checkbox, Description) rather than free text; **not** counted toward profile completion (matches Industries/LinkedIn/Portfolio/Salary/Preferred Locations — none of those are required either); **not** added to Feature 07's AI extraction (manual entry only, keeps the already-shipped extraction schema/prompt untouched). New `profiles.academic_experience jsonb NOT NULL DEFAULT '[]'::jsonb` column, migration applied live via `run-raw-sql` and confirmed via `information_schema.columns` (same pattern as `work_experience`/`education`, not a fresh design). New `AcademicExperienceCard.tsx` (mirrors `WorkExperienceRoleCard.tsx`; up to 5 entries, no "at least one" minimum since this is opt-in). `types/index.ts` (`AcademicExperienceType`/`AcademicExperienceEntry`), `lib/profile-transform.ts` (`AcademicExperienceRecord`, `toAcademicExperienceRecords`/`fromAcademicExperienceRecord`, `ProfileRow`/`fromProfileRow` both updated), `actions/profile.ts` (`academicExperienceEntrySchema`, added to the upsert payload) all updated to match. See architecture.md's Feature 06 decision, "Academic Experience added" addendum. Verified with `npx tsc --noEmit`, `npm run lint`, `npm run build` — all clean. **Not yet driven through the real running app** — same category as Feature 08's initial self-check; the engineer should add an entry as a student and confirm it saves and reloads correctly.

- **`app/dashboard/page.tsx` is a placeholder, not Feature 14.** Added out of sequence, at the user's explicit request, only so the OAuth login flow (which redirects to `/dashboard` per architecture.md) doesn't 404 while testing Feature 05. It's a single static card ("Dashboard coming soon" + a link to `/profile`) — no stats bar, no recent activity, no analytics. **Do not treat Phase 5 as started.** When Feature 14 is actually built, this file gets replaced wholesale, not extended.

- **2026-09-07 — Wired up the logout control flagged as missing since Feature 03.** `signOutAction`/`SignOutButton` (built in Features 02/03) had no caller anywhere in the UI — Feature 03's notes explicitly deferred this as scope creep at the time ("the Navbar isn't session-aware"). Made `Navbar` accept an `isAuthenticated?: boolean` prop (default `false`, so the homepage's existing "Start for free" CTA is unchanged) rather than resolving the session client-side in the Navbar itself — every page that renders it already knows its own auth state server-side (`/` redirects away when logged in; `/dashboard` and `/profile` are gated by `proxy.ts` so they only ever render logged in), so this avoids a logged-in/out flash. `app/profile/page.tsx` passes `isAuthenticated={Boolean(user)}` (it already fetches `user`); `app/dashboard/page.tsx` passes a static `isAuthenticated` (it doesn't fetch the session itself — `proxy.ts` is the only gate there, unchanged from the Feature 05 placeholder). When authenticated, the navbar's right-side CTA becomes a secondary-style "Log out" button (`SignOutButton` + `LogOut` icon) instead of "Start for free" — same `<form action={signOutAction}>` pattern as `LoginCard`'s OAuth buttons, so `resetIdentity()` (PostHog) already fires on submit with no new wiring needed. See `ui-registry.md`'s `Navbar` entry. Verified with `npx tsc --noEmit` (clean); no live click-through in this sandbox (same `proxy.ts`-gated-route limitation as Features 05/07/08) — the engineer should confirm the button appears on `/dashboard` and `/profile` and actually signs out.
- **2026-09-07 — Hardened resume generation and profile schema migrations:** added the version controlled academic experience migration with an empty array default and non null constraint; resume preview now requires the stored object key and gives URL only legacy records a clear regeneration path; generation uses an optimistic pointer update, cleans up failed uploads, and retries storage deletion so concurrent requests cannot remove the current resume. The Generate button is also disabled while profile saving is pending. Verified with `npx tsc --noEmit` and `npm run lint`.

### 11 Filter + Sort + Pagination

- **`/architect` pass first**: the one load-bearing question left open by `build-plan.md`/Feature 10's Decision 12 — client-side over the jobs the page already loads, vs. server-side via URL params re-querying the DB on every change — was put to the engineer directly. **Decision: client-side.** Full rationale and every implementation call that follows from it (filter/search/sort/page-reset order, the page-number/ellipsis rule, the new empty state) lives in `architecture.md`'s "Filter + Sort + Pagination" section.
- Two new pure-function helper files, colocated with their one consumer per this project's existing "don't promote until a second consumer needs it" precedent: `components/find-jobs/filter-jobs.ts` (`FilterOption`, `filterJobs()` reusing the existing `MATCH_THRESHOLD` — never a second hardcoded `70` — `searchJobs()`, case-insensitive company/role substring match) and `components/find-jobs/paginate-jobs.ts` (`JOBS_PAGE_SIZE = 20`, `paginateJobs()`, `getPageNumbers()` — the show-first/last/current±1/collapse-the-rest-into-one-ellipsis rule `JobsPagination` previously faked as a hardcoded "1 2 3 … 8").
- `JobsListSection.tsx` now owns `filterBy`/`searchQuery`/`currentPage` alongside the `sortBy` state it already had (Feature 09) — same "one small client island" pattern, no new client components. The pipeline is filter → search → sort → paginate; changing the filter, search text, or sort resets the page back to 1.
- `JobFilters.tsx`'s "All Matches" select and text input, both previously uncontrolled/dead, are now fully controlled (`filterBy`/`onFilterChange`, `searchQuery`/`onSearchChange`) — same treatment `sortBy`/`onSortChange` already had.
- `JobsPagination.tsx` is real now: `totalCount`/`currentPage`/`onPageChange` props drive the "Showing X to Y of Z" text, Previous/Next enable/disable correctly at the ends, and page numbers render from `getPageNumbers()` instead of literal JSX.
- Added a second, distinct empty state (`NoMatchesEmptyState`, local to `JobsListSection.tsx`) for "saved jobs exist but the current filter/search narrows them to zero" — separate from Feature 09's `JobsEmptyState` ("no jobs saved yet"), since a zero-row table with a "Showing 1 to 0 of 0" caption would otherwise look broken rather than intentional.
- No new PostHog event, no DB read/write — matches `build-plan.md`'s Feature 11 entry (pure client-side wiring over data Feature 10 already loads).
- Verified with `npx tsc --noEmit` (clean), `npm run lint` (clean), `npm run build` (clean production build, `/find-jobs` still listed as a route). **Not yet click-tested through the real running app** — same sandbox limitation as every prior feature; the engineer should search for enough jobs to exceed one page, then confirm text search, the High/Low Match filter, sort, and pagination (including Previous/Next and the ellipsis at >7 pages) all behave as described in `architecture.md`'s Verify checklist.

### 12 Job Details Page — Full UI

- **Step 0 gate: no decision owed, built directly.** Every value the design needs already has a named source — the `jobs` table (architecture.md's Feature 04 migration) already carries every column the page shows (`title`, `company`, `location`, `salary`, `job_type`, `about_role`, `match_score`, `match_reason`, `matched_skills`, `missing_skills`, `external_apply_url`), and Feature 10's insert path already populates all of them. `build-plan.md`'s Feature 12 entry says to wire real data immediately and show the Company Research card's empty state only — no provider, schema, or behavior choice was left open.
- Built pixel-by-pixel against `context/designs/job-details.png`, with colors and shapes pixel-sampled directly (PIL), not eyeballed — every sampled color matched an existing `ui-tokens.md` token exactly, no new tokens introduced. New `components/job-details/{JobInfo,MatchScore,JobDescription,CompanyResearch,JobActions}.tsx` (the exact file set `architecture.md`'s folder structure already named for this feature) and `app/find-jobs/[id]/page.tsx` (new dynamic route). See `ui-registry.md` for each component's classes.
- **New types, kept separate from Feature 10's `Job`/`JobRow` rather than extending them**: `JobDetail`/`JobType` (`types/index.ts`) and `JobDetailRow`/`fromJobDetailRow` (`lib/job-transform.ts`). `Job`/`fromJobRow` already have a real consumer (`JobsTable`) that only needs the find-jobs table's six fields — growing that type to carry the detail page's dozen-plus fields would make every table row carry data it never renders. Mirrors this project's existing "don't widen a shared type when the two views need genuinely disjoint slices of the same row" precedent.
- `app/find-jobs/[id]/page.tsx`: `params` is `Promise<{ id: string }>`, awaited per Next.js 16's dynamic route contract (confirmed directly against `node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/dynamic-routes.md`, not assumed from training data — this repo's `next dev`-injected `AGENTS.md` note says to). Same defensive `redirect("/login")` fallback as `app/find-jobs/page.tsx` (`proxy.ts` already guards `/find-jobs/*`). Query: `.eq("id", id).eq("user_id", user.id).maybeSingle()` — `.maybeSingle()` isn't mentioned in `library-docs.md`'s DB Queries section (only `.single()` is), but `insforge.database.from()` returns a real `@supabase/postgrest-js` `PostgrestQueryBuilder` (confirmed against `node_modules/@insforge/sdk/dist/client-DZHoCptg.d.ts` and `node_modules/@supabase/postgrest-js/dist/cjs/PostgrestTransformBuilder.d.ts` directly), which does expose `.maybeSingle()` — the correct method here since "no such job, or it belongs to someone else" is an expected case, not an error, and `.single()`'s PostgREST semantics treat zero rows as an error. Missing/foreign job → `notFound()` (`next/navigation`), same real-404 pattern the Next.js docs show, no custom `not-found.tsx` added (out of scope, default Next.js 404 is enough for this feature).
- **`JobsTable.tsx` rows are now real links to `/find-jobs/[id]`** — a necessary enabling change, not scope creep: without it the new page has no way to reach it from the app itself, and `project-overview.md`'s "Click job row → opens job details page" was never wired by any prior feature. Used the "stretched link" pattern (`position: relative` on the `<tr>`, an absolutely-positioned `next/link` `<Link>` as the first child of the row's first `<td>`) instead of an `onClick` handler, so `JobsTable` stays a Server Component and the row stays a real, keyboard-operable navigation primitive (`/develop`'s accessibility checklist requires navigation use the platform's real primitive, not a styled container faking one) rather than a client-side `useRouter().push()`.
- **Two real, pixel-sampled deviations from the design's generic prose elsewhere in this project, both scoped to one component, neither a global correction:**
  - Job Type renders `"—"` when `job.jobType` is null (the design's own sample row shows exactly this state) — `jobs.job_type` is nullable even though `mapAdzunaJobType` (Feature 10) currently always resolves a value, so this is real null-handling for a real nullable column, not an invented state.
  - The "Research Company" button uses `rounded-full` (a visibly larger corner radius than every other primary button in the app, pixel-confirmed). Scoped to `CompanyResearch.tsx` only — `ui-rules.md`'s general Buttons section is untouched, since nothing else shares this shape yet (unlike Feature 09's navbar-underline fix, which was a real cross-page norm).
- Job Description renders `about_role` as a single raw text block, matching both the design and `build-plan.md`'s Feature 12 UI list exactly — the separate Responsibilities/Requirements/Nice to have/Benefits bullet sections `project-overview.md`'s older flow-description prose lists were never part of this feature's actual build spec, and `about_role` is the only one of those `jobs` columns Feature 10 populates today (the array columns stay empty defaults). Same "design PNG + build-plan.md override older prose" precedent as Features 01/05/09.
- No new PostHog event (not in `code-standards.md`'s six-event list, not called for by `build-plan.md`), no new dependency.
- Verified with `npx tsc --noEmit` (clean), `npm run lint` (clean), `npm run build` (clean production build, `/find-jobs/[id]` listed as a dynamic route). **Not click-tested through the real running app** — same sandbox limitation as every prior feature. Before moving to Feature 13, the engineer should: open a real job from `/find-jobs` via a table row click, confirm the header/info cards/match reasoning/skills/description all show the right real data, confirm "View Job Post" and "Apply Now" both open the same `external_apply_url` in a new tab, confirm "Back to Jobs" returns to `/find-jobs`, and confirm visiting `/find-jobs/<some-other-users-job-id>` (or a made-up id) renders the 404 page rather than leaking another user's job.

- **2026-09-09 — Added a truncated-preview notice to `JobDescription.tsx`, on request, from a shared screenshot (not part of the original `job-details.png`).** `context/library-docs.md`'s Adzuna API section already documents `about_role` as "a snippet — not full description," cut off mid-sentence with no explicit truncation flag from Adzuna itself — this closes a real, previously-documented gap rather than inventing a new one. A local `looksTruncated()` heuristic (trailing `...`/`…`, or no real terminal punctuation, counts as cut off) decides whether to show a muted callout — explanatory text plus a "View Full Job Post" link reusing the job's existing `external_apply_url` (no new `source_url` field added; both columns hold the same Adzuna `redirect_url` today) — below the description text. Only shows when there's a description at all and it looks cut off; never on top of the "No description provided" empty state. See `ui-registry.md`'s `JobDescription` entry for the exact heuristic and classes. Re-verified: `npx tsc --noEmit`, `npm run lint`, `npm run build` all clean. **Not click-tested live** — same sandbox limitation as the rest of this feature; the engineer should open a job whose Adzuna snippet actually got cut off and confirm the callout and link both appear and work.
